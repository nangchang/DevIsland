interface:
  display_name: "DevIsland Hook Providers"
  short_description: "Handle Claude, Codex, and Gemini hook semantics in DevIsland."
  default_prompt: "Use this skill when changing DevIsland CLI hook provider behavior."
